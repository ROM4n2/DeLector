# fastapi/starlette 必须 pin：CI 实测 starlette 1.6.0（随 fastapi>=0.141 漂移进来）
# 下 include_router 后 APIRoute.endpoint 不再保持原函数对象，该守卫测试
# （test_register_routes_covers_every_module_in_routes_package）曾据对象身份比对
# 把全部路由误判为 missing（2026-09-09 v5.4.0 发版 CI 三平台全炸的根因）。
# 该守卫现已改用 HTTP 语义键：(完整 path, METHOD)，已注册侧取自 app.openapi()["paths"]。
# 升级前先本地全量 pytest 验证该守卫测试。
fastapi==0.136.3
starlette==1.6.0
uvicorn>=0.53.0
spacy>=3.8.16
httpx>=0.28.1
genanki>=0.13.1
pytest>=9.1.1
edge-tts>=7.2.8
